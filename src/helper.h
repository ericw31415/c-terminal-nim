#ifndef HELPER_H
#define HELPER_H

// Return a uniform random integer in the range of min to max inclusive
int rand_int(int min, int max);

// Strip the first newline from a string
char *strip_newline(char *str);

#endif
